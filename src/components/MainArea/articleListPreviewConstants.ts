/** Delay after scroll stops before new list preview thumbnails start loading. */
export const ARTICLE_LIST_PREVIEW_SCROLL_IDLE_MS = 150;
/** Brief defer after a source switch so keyboard navigation stays smooth at the top. */
export const ARTICLE_LIST_SOURCE_SWITCH_PREVIEW_DEFER_MS = 300;
